# 0001 — system: initial generation

- **Date**: 2026-09-17
- **Module**: `system` — full initial generation covering all three modules
  (URL creation, Redirect, Metadata) plus the cross-cutting expiration rule.
- **Spec version**: working tree at time of generation, pre-first-commit for
  the implementation itself. The last spec-only commit in this repository's
  history prior to generation was `41df69b` ("Rename ADRs to
  ADR-NNNN-title.md convention"); `blueprint/specs/url-creation.md` was
  further amended mid-generation (see Result below) beyond that commit,
  before the implementation was finalized.
- **Acceptance version**: all four `blueprint/features/*.feature` files
  (`url-creation.feature`, `redirects.feature`, `expiration.feature`,
  `metadata.feature`) and every entry in `blueprint/GUARANTEES.md`, as they
  existed at generation time — including the two scenarios added to
  `url-creation.feature` mid-generation for the `original_url` host/control-
  character validation gap (found during code review).
- **Trigger**: initial generation — no prior implementation existed.
- **Agent/author**: generated by an orchestrated multi-agent workflow —
  Architect (module and concurrency design), Developer (implementation),
  Tester (feature-scenario and property-based guarantee tests), Code
  Reviewer and Security Auditor (hardening passes) — coordinated by an
  orchestrating agent, working from this repository's specs, contracts,
  features, and guarantees.
- **Result**: all scenarios in all four `.feature` files pass. All
  applicable `blueprint/GUARANTEES.md` entries hold, verified via Hypothesis
  property tests. A gap was found during code review: `specs/url-creation.md`
  was silent on `original_url` values with an empty host (e.g. `https://`
  alone) or embedded control characters (`\r`, `\n`, other C0/C1). Closed by
  adding two explicit business rules to `blueprint/specs/url-creation.md` and
  two corresponding scenarios to `blueprint/features/url-creation.feature`
  ("Scheme-only URL with no host is rejected" and "original_url containing
  control characters is rejected"), then implementing and testing both.
  Implementation choice (noted for context only, not binding on future
  regenerations): Python 3.11+, FastAPI, in-memory single-process
  thread-safe store. All Critical/Should-fix code-review findings were
  resolved; three Medium-severity security-audit findings remain open as of
  this entry: a host-validation edge case (userinfo-only URLs), no request
  body size limit, and a Location-header percent-encoding question relative
  to `GUARANTEES.md`'s round-trip wording — tracked for follow-up, not
  blocking.
